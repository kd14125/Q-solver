# 更新日志

本文件记录 Q-Solver 增强版的重要功能变化。

## [1.3.1] - 2026-08-30

### 新增

- 新增 AI 回复字体颜色选项，可在白色和黑色之间切换；正文、标题、列表、代码块与行号统一生效。
- 新增 `aiTextColor` 持久化配置，仅接受 `white` 或 `black`；旧配置自动补齐白色默认值。

### 改进

- 窗口移动和内容滚动快捷键支持点按执行一次、长按跟随 Windows 键盘重复速率持续执行。
- 截图、发送、显示切换和鼠标穿透等一次性快捷键在长按时仍只触发一次。
- 透明模式下的 Toast、状态灯、加载/成功状态和思考区域统一改为无彩色背景、无边框、无阴影和无发光效果。
- AI 字体颜色切换支持即时预览；取消设置时会恢复已保存的颜色与文字透明度。

### 修复

- 移除模型返回失败时的大型红色图标、阴影、淡入与脉冲动画，仅保留跟随 AI 字体颜色和透明度的简洁错误文字。
- 修复任意非零窗口透明度下，错误、成功、警告或信息状态仍可能突然显示明显彩色色块的问题。

### 测试

- 新增长按重复动作与一次性动作去重测试，并覆盖全部移动、滚动、截图、发送及界面切换动作分类。
- 新增 AI 字体颜色旧配置迁移、黑色持久化及非法颜色拒绝测试。
- 通过快捷键压力测试、配置竞态检测、全量 Go 测试、前端生产构建与 Windows EXE 启动检查。

## [1.3.0] - 2026-08-30

### 新增

- 新增 `F6`“隐藏/显示界面内容”快捷键，支持在设置中重新录制；隐藏后可用同一快捷键恢复。
- 新增顶部工具栏和历史问题栏独立隐藏开关，并将欢迎页 Logo、标题、快捷卡片和底部提示纳入总隐藏模式。
- 新增 AI 回复文字透明度滑块，范围为 0%–95%，保存后持久化生效。
- 新增高透明和特别透明两档内部组件样式，窗口透明时工具栏、卡片、回答区和状态区域同步变透明。
- 新增单实例锁，重复启动时复用已有窗口。
- 新增稳定的用户配置路径 `%APPDATA%/Q-Solver/config.json`，自动迁移 EXE 旁或旧工作目录中的配置。

### 改进

- 截图缓存提示精简为 `1/3`、`2/3`、`3/3`，移除缩略图、删除、清空及操作按钮；截图与发送继续通过全局快捷键完成。
- AI 回答正文、标题、列表、行内代码、代码块、语法高亮及行号统一改为白色。
- AI 回答区、行内代码和代码块背景强制完全透明，移除绿色胶囊块、代码块顶部装饰和有色阴影。
- 移除发送问题后的“深度思考中”圆环、计时器和追加回复动画。
- 完全透明的 `opacity=0` 现在可以正确写入并重新加载。
- 语音面试模式和截图答题保持独立，开启实时语音时仍可继续使用截图与发送功能。

### 修复

- 修复快捷键录制预览提前覆盖正在生效的快捷键。
- 修复录制完成后前端未退出录制状态，导致后续全局快捷键被持续吞掉。
- 修复录制 F1、修改透明度、打开/关闭设置或保存设置后 F7/F8/F9 及 Alt 组合键失效。
- 修复 Windows 偶尔漏收 `KEYUP` 后残留按键污染所有后续组合键的问题。
- 修复 Hook 协程可能迁移到其他系统线程导致全局快捷键随机失效。
- 修复异步快捷键回调乱序、阻塞低级 Hook 及回调异常导致程序退出的风险。
- 修复每次 Hook 重启重复分配 Windows 回调跳板造成的长期资源泄漏。
- 修复 AI 文字透明度滑块方向相反的问题，并自动迁移上一版误保存的 `aiTextOpacity` 数值。
- 修复前端保存失败时界面快捷键与后端实际配置不一致，以及旧快捷键字段无法删除的问题。

### 测试

- 新增录制完成、状态清理、回调顺序、遗漏抬键恢复及配置订阅重启 Hook 测试。
- 新增 F1/F7/F9/Alt 方向键连续触发与 Hook 重启压力测试。
- 新增配置路径迁移、完全透明持久化和 AI 文字透明度兼容迁移测试。

## [1.2.0] - 2026-08-30

### 新增

- 新增独立的 Qwen3.5 Omni Realtime 语音面试 API，默认模型为 `qwen3.5-omni-plus-realtime`。
- 新增语音专属 API Key、Workspace ID、地域、Base URL、模型、提示词、生成参数及 Semantic VAD 配置。
- 新增语音 API 连接测试；截图答题和语音面试两套设置完全隔离并分别持久化。
- 新增 Windows WASAPI Loopback 系统音频采集，只捕获默认播放设备输出，不直接采集麦克风。
- 新增 Semantic VAD 自动断句，面试官说完后自动生成候选人可直接参考的简短口语化文字回答。
- 新增 Qwen Realtime 转录预览、最终问题与流式回答事件映射，并避免问题和答案重复显示。
- 新增 1–3 张截图缓存，截图与发送拆分为两个独立快捷键。
- 新增窗口四边及四角缩放，并将最小窗口尺寸缩小为原来的一半。
- 新增 AI 回复字体大小调节。
- 新增代码块自动换行开关与代码行号显示。
- 新增白天主题，支持在设置中即时预览，并与夜间主题一起持久化保存。
- 新增通用解题默认提示词，以及针对面试场景优化的短回答语音提示词。

### 修复

- 修复 `Alt + ←`、`Alt + ↓` 等方向移动快捷键可能导致应用闪退的问题。
- 修复代码块加入行号后代码内容不显示的问题。
- 修复长代码只能横向滚动、透明窗口中代码块背景不透明的问题。
- 修复无截图时仍显示截图缓存区域的问题。
- 加强 WebSocket 并发写保护、停止清理、有限重连与主动停止判断。
- 修复转录 delta、最终转录及回答完成事件可能造成的重复内容。

### 兼容性

- 保留原有截图答题、Gemini、OpenAI-compatible、自定义接口及 Gemini Live 功能。
- 旧配置自动加载新字段默认值，不覆盖用户已有 API、提示词和生成参数。
- Qwen Realtime 与旧版第三方 STT 模式互斥，防止同时采集和发送音频。

## [1.0.0]

- 上游项目初始公开版本。

[1.3.1]: https://github.com/kd14125/Q-solver/releases/tag/v1.3.1
[1.3.0]: https://github.com/kd14125/Q-solver/releases/tag/v1.3.0
[1.2.0]: https://github.com/kd14125/Q-solver/releases/tag/v1.2.0
[1.0.0]: https://github.com/kd14125/Q-solver/releases/tag/v1.0.0
